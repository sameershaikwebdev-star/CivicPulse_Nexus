# Base API URL for backend calls
# Local development (leave empty or use /api if proxying through Vite):
# VITE_API_URL=/api

# Production on Vercel (point to your deployed Render backend URL):
VITE_API_URL=https://your-backend-name.onrender.com/api
